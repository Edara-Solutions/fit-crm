export type Status = 'active' | 'inactive';

export type SelectOption = {
  label: string;
  value: string;
};

export type TimelineItem = {
  label: string;
  date: string;
  description: string;
};
